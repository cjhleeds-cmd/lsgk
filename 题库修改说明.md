# 历史长河：题库修改说明

## 最简单的修改方法

1. 双击打开 `题库编辑器.html`。
2. 在左侧选择“历史地图”和“章节”。
3. 修改题干、四个选项、正确答案、题目类型、答案解析或来源。
4. 可以使用每道题右上角的“复制”或“删除”，也可以点击左侧“添加一道题”。
5. 修改过程会自动保存为浏览器草稿。
6. 修改完成后，点击右上角“导出题库.js”。
7. 把下载得到的 `题库.js` 放进本项目的 `data` 文件夹，替换原文件。
8. 重新打开 `历史长河.html` 检查修改结果。

题库编辑器适合临时核对和修正文案。正式增删题目时，应同时维护 `documents/` 与 `tools/paper-config.json`，再运行构建脚本；否则 `build-history-bank.mjs --check` 会提示现役题库不是由权威 Markdown 重建的结果。当前项目不接受自行生成的新题进入正式练习题库。

## 项目结构

- `历史长河.html`：游戏入口。
- `题库编辑器.html`：可视化题库管理工具。
- `data/题库.js`：考试大纲、整卷档案、全部地图、章节、题目和图片对应关系。
- `documents/考试大纲.md`：联考历史考试大纲原始 Markdown。
- `documents/试卷/`：2022—2025年真题、2025年两套模拟题和2026年两套模拟题。
- `tools/paper-config.json`：八份试卷的答案、章节归类和解析配置。
- `tools/distractor-notes.json`：相关或容易混淆的错误项简短辨析；不存放正确项的重复解析。
- `tools/build-history-bank.mjs`：从试卷和考纲原始 Markdown 重建可追溯无图题库；`--check` 只校验、不写文件。
- `tools/check-project.mjs`：检查题库、试卷档案、历史现场、图片和页面引用关系。
- `tools/smoke-game.mjs`：检查答题、错题订正、关卡解锁、口述反馈和印章奖励等核心流程。
- `js/app.js`：抽题、答题、评分、解锁、错题集等程序逻辑。
- `css/styles.css`：全部界面样式和排版。
- `assets/`：38张首页、章节和历史现场WebP图片。

## 直接编辑题库文件

也可以用文本编辑器打开 `data/题库.js`。每道题的基本结构如下：

```js
{
  "type": "basic",
  "q": "题干",
  "opts": ["选项A", "选项B", "选项C", "选项D"],
  "ans": 0,
  "explanation": "答案解析\n【来源】原始文件与题号",
  "optionExplanations": ["", "B项易混辨析", "", "D项易混辨析"],
  "source": "题目来源"
}
```

`ans` 中的 `0、1、2、3` 分别代表 `A、B、C、D`。`explanation` 只写一次正确答案的总解析；`optionExplanations` 只给相关或容易混淆的错误项写简短辨析，正确项及无需单独解释的错误项留空。辨析不要只写“与题意不符”，应尽量补充可核对的时间、人物、制度或事件，例如“甲骨文在商代已高度发展”“行星运动三定律由开普勒提出”。直接编辑时要注意英文逗号、引号和括号；使用题库编辑器则不需要处理这些格式。

## 批量上传整份试卷

1. 打开 `题库编辑器.html`，点击右上角“批量导入试卷”。
2. 一次选择多份 `.md` 文件，或把文件拖到上传区域。
3. 编辑器会识别 `### 题号`、题干和四个 `- A. 选项`，并初步匹配历史地图和章节。
4. 在预览列表中逐题核对地图、章节和正确答案。
5. 没有答案时，可在每份试卷上方输入连续答案串，如 `BCDACB...`，再点击“应用答案串”。
6. 点击“归档并导入已填答案的选择题”。
7. 完成后点击右上角“导出题库.js”，并替换 `data/题库.js`。

导入规则：

- 有四个选项且有正确答案的题目会进入练习题库。
- 没有答案的选择题不会进入自动判分题库。
- 材料解析题和问答题会保存在整卷档案中。
- 相同题干会被识别为重复题，只补充来源，不重复添加。
- 自动章节归类只是初步判断，导入前应人工核对。
- 发布前运行 `build-history-bank.mjs`；依赖图片、表格或示意图的题，以及人工排除的争议题，不会进入 `MAPS` 练习题库。

## 当前资料与题库规模

- 8份整卷档案，每份20道选择题、5道主观题。
- 现役练习题全部精确来自八份试卷或考试大纲后半部分题型示例。
- 中国古代史44道、中国近现代史46道、世界史50道。
- 合计140道；每题均有来源和总解析，相关或易混错误项另有简短辨析，现役题中没有图表依赖或重复题干。
- 20道图表依赖题和1道答案表述有明显史实争议的模拟题只在整卷原始档案中保留，不进入练习。
- “中古时期的世界”目前没有符合全部筛选条件的题，游戏会标记并跳过该空章节。

新加入的2025年两套模拟题原始 Markdown 均附有参考答案，其中二模还附逐题解析；其余六份试卷未附完整标准答案，相关答案与解析仍是人工整理，尚待取得命题方答案后再次核对。不要为填满章节而自行生成题目。

## 修改后的检查

在项目根目录运行：

```bash
node tools/build-history-bank.mjs --check
node tools/check-project.mjs
node tools/smoke-game.mjs
```

- 第一条确认现役题库与试卷、考纲及人工排除规则重建后的结果一致。
- 第二条检查数据结构、来源、总解析与易混项辨析、图表残留、重复题、来源档案、历史现场和素材引用。
- 第三条检查用户实际使用的核心流程。

检查通过只证明当前文件关系和程序逻辑可运行，不等于试卷答案已经得到官方认证。

## 注意事项

- 不要单独移动 HTML；应保留整个项目文件夹的目录结构。
- 导出题库后，只有替换 `data/题库.js`，游戏才会使用新题库。
- 编辑器草稿只保存在当前浏览器中，重要修改请及时导出。
- 本轮题库使用新的 `historyQuizEditorDraftV3` 草稿键，避免旧版草稿覆盖可追溯题库。
- 整卷 Markdown 也已保存在 `documents/试卷/`，建议与导出的题库文件一起提交到 GitHub。
- 题目图片与首页图片不在题库编辑器中修改，请直接替换 `assets` 内同名图片。
- `data/题库.js` 同时包含题库与整卷归档；使用题库编辑器导出或构建脚本写入后，都应重新运行上述三项检查。
